using Xunit;

namespace Yatzy.Tests
{
    public class PlayerTests
    {
        [Fact]
        public void ShouldRollDice()
        {
            // var player = new Player();

            // We need to check if roll() is called in the Dice class.
        }
    }
}